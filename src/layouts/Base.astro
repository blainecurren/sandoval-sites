---
import '../styles/global.css';
import { business } from '../data/content';

interface Props {
  title: string;
  description?: string;
  /** body class hook so each variant can set its base background/text color */
  bodyClass?: string;
}

// BASE_URL carries a trailing slash; trim it so `${base}/favicon.svg` is clean.
const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const { title, description = business.tagline, bodyClass = '' } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" href={`${base}/favicon.svg`} type="image/svg+xml" />
  </head>
  <body class={bodyClass}>
    <slot />
  </body>
</html>
